#pragma once

#include <glm/glm.hpp>

// Player
const glm::vec3 PLAYER_SPAWN_POS = glm::vec3(10.0f, 1.5f, 0.0f);
const float     GRAVITY          = 15.75f;

// Camera
const float YAW         = -90.0f;
const float PITCH       = 0.0f;
const float SENSITIVITY = 0.1f;
const float ZOOM        = 45.0f;
